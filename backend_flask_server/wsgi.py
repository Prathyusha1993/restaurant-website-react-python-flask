from main import app

if __name__ == '__main__':
    app.run(debug=True)




# Gunicorn configuration and WSGI(Web Server Gateway Interface) entry point are both compojnets use din deploying and serving
# python web applications, particularly those built with frameworks like Flask or Django.